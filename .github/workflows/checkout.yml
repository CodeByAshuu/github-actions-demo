name: Checkout Demo

on:
    push

jobs:
    checkout-job:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Repository
              uses : actions/checkout@v4

            - name: Show Content
              run: cat hello.txt
